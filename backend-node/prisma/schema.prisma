generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Resume {
  id        Int      @id @default(autoincrement())
  userId    String
  original  String
  result    String
  createdAt DateTime @default(now())
}
